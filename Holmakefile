INCLUDES = $(HOLDIR)/src/emit/ML/

THYFILES = $(patsubst %Script.sml,%Theory.uo,$(wildcard *.sml))
TARGETS = $(patsubst %.sml,%.uo,$(THYFILES))

default: all

regexML.sml: regexEmit.uo
	./runhol.sh regexEmit.sml

regexML.sig: regexML.sml

all: $(TARGETS) regexML.ui regexML.uo

.PHONY: all default
